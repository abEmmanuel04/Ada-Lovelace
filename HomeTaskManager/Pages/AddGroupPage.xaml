<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HomeTaskManager.Pages.AddGroupPage"
             Title="Add Group">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Back" Clicked="OnBackClicked" Priority="0" Order="Primary" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="28">
            <Label Text="Grow your support squad" FontAttributes="Bold" FontSize="28" />
            <Label Text="Create a new group or join one with a code. Everyone you invite will be able to add and edit shared goals." FontSize="16" TextColor="{StaticResource Gray600}" />

            <Frame Padding="20" BackgroundColor="{StaticResource Secondary}" CornerRadius="20" HasShadow="False" >
                <VerticalStackLayout Spacing="16">
                    <Label Text="Create a group" FontAttributes="Bold" FontSize="20" />
                    <Entry x:Name="GroupNameEntry" Placeholder="Group name" />
                    <Editor x:Name="GroupFocusEditor" Placeholder="What should this group focus on?" AutoSize="TextChanges" />
                    <Button Text="Create" Command="{Binding CreateGroupCommand}" />
                </VerticalStackLayout>
            </Frame>

            <Frame Padding="20" BackgroundColor="{StaticResource Secondary}" CornerRadius="20" HasShadow="False">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Share your invite code" FontAttributes="Bold" FontSize="20" />
                    <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                        <Label Text="{Binding GeneratedCode}" FontAttributes="Bold" FontSize="24" />
                        <Button Text="Copy" Command="{Binding CopyCodeCommand}" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <Frame Padding="20" BackgroundColor="{StaticResource Secondary}" CornerRadius="20" HasShadow="False">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Join a group" FontAttributes="Bold" FontSize="20" />
                    <Entry x:Name="JoinCodeEntry" Placeholder="Enter invite code" />
                    <Button Text="Join" Command="{Binding JoinGroupCommand}" />
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
